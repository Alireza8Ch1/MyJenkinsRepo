pipeline {
    
    agent any
    stages {
          options {
        timeout(time: 10, unit: 'SECONDS') 
    }
        stage('Example') {
            steps {
                input 'procceed??'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
