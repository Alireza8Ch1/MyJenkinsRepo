pipeline {
     options {
        timeout(time: 10, unit: 'seconds') 
    }
    agent any
    stages {
        stage('Example') {
            steps {
                input 'procceed??'
            }
        }
    }
    post { 
        always { 
            echo 'I will always say Hello again!'
        }
    }
}
